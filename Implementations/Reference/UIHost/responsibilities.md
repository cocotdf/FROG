# UIHost Responsibilities

- Bind runtime execution to host-side widget behavior when declared by the contract
- Support widget_value, widget_reference, and standardized UI primitives for the first slice
- Preserve natural value participation versus object-style interaction
- Resolve widget handles and host-side property / method dispatch
- Keep toolkit-specific behavior private to the reference family
